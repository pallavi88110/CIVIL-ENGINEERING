# train_model.py

from ultralytics import YOLO

# Load pretrained model
model = YOLO("yolov8n.pt")

# Train on your dataset
model.train(
    data="construction_dataset.yaml",
    epochs=50,
    imgsz=640
)

# Save model (best.pt will be inside runs folder)
